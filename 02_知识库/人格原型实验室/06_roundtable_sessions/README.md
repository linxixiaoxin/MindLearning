# Roundtable Sessions

这里保存未来的圆桌推演记录。

每个会话应记录：

- 使用了哪个 `VirtualPerson`；
- 使用了哪些 `ArchetypeKernel`；
- 进入了哪个 `LifeScene`；
- 是否启用了 `RuleSet`；
- 圆桌角色的发言；
- 用户或专家的校正；
- 最小行动；
- 输出物。

真实用户会话默认不进入公开导出，需要标注隐私级别。
