# Source Texts

这里不直接存放大段原文，优先保存指向 `01_sources/01_books/` 的路径、抽取状态和证据索引。

原因：

- 避免重复存储版权文本；
- 方便区分内部研究和公开输出；
- 让前端只读取结构化对象，不直接读取原始书籍。

当一个作品进入抽取流程时，应先在 `00_registry/source_works.json` 登记，再把抽取后的原型放入 `02_archetype_kernels/`。
